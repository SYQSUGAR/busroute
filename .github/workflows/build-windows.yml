name: build-windows

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test
        run: |
          python -m unittest discover -s tests -v

      - name: Build
        run: |
          pyinstaller --noconfirm --clean --windowed --name TransitCollector --collect-all keyring --collect-all geopandas --collect-all pyogrio --collect-all pyproj --collect-all shapely --collect-all transbigdata app.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: TransitCollector-windows
          path: dist/TransitCollector/
